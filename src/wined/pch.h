// pch.h: This is a precompiled header file.
// The files listed below are compiled only once, which improves the performance of generating future builds.
// This also affects IntelliSense performance, including code completion and many code navigation features.
// However, the files listed here are ALL recompiled if any of them are updated between builds.
// Don't add frequently updated files here, as that negates performance gains.

#ifndef PCH_H
#define PCH_H

// add headers to precompile here
#include <pdcwin.h>

#endif //PCH_H